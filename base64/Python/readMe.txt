Add the .py file to the directory that has the files you need to encode
Update the file_extension variable, replace "your_file_extension" with the file extension you need to encode
Save the changes
Open a shell to the directory where the script and files are
Run: python batchb64.py
Encoded .txt files will be created in the same directory with the same name as the original files
